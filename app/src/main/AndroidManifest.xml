<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digis.quwa.test">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.QuwaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quwa">
        <activity
            android:name=".app.main.MainActivity"
            android:theme="@style/Theme.Quwa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.auth.AuthActivity"
            android:theme="@style/Theme.Quwa" />

        <activity
            android:name=".app.projects.ProjectListActivity"
            android:theme="@style/Theme.Quwa" />

        <activity
            android:name=".app.projects.users.UsersActivity"
            android:theme="@style/Theme.Quwa" />

    </application>

</manifest>